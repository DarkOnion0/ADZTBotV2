# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Remove the track of the build folder
bin/
dist/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Remove the tracking of editor config file
.idea
.vscode
.vim

# Remove the tracking of env file
.envrc
.env

# Don't track the tag files generated by my neovim config
tags

# Ignore DevSpace cache and log folder
.devspace/
